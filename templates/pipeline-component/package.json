{
  "name": "{{PIPELINE_ID}}",
  "version": "0.1.0",
  "description": "Harper pipeline for {{SOURCE_NAME}}, built by OpenClaw.",
  "type": "module",
  "files": [
    "schema.graphql",
    "config.yaml",
    "resources.js"
  ],
  "dependencies": {
    "cron-parser": "^5.0.0"
  },
  "scripts": {
    "deploy": "dotenv -- harperdb deploy_component . restart=rolling replicated=true"
  },
  "license": "Apache-2.0"
}
