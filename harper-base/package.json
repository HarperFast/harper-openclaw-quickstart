{
  "name": "harper-base",
  "version": "0.1.0",
  "description": "Registry + human-escape-hatch tables for the OpenClaw→Harper pipeline-builder pattern.",
  "type": "module",
  "scripts": {
    "deploy": "npm run deploy:preflight && dotenv -- npm run deploy:component && npm run deploy:verify",
    "deploy:preflight": "dotenv -- node ./scripts/preflight.mjs",
    "deploy:component": "harperdb deploy_component . restart=rolling replicated=true",
    "deploy:verify": "dotenv -- node ./scripts/verify.mjs"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "harperdb": "^4.7.20"
  },
  "license": "Apache-2.0"
}
